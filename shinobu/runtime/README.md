# Shinobu Runtime
Runtime acts as the "core" to Shinobu. It is responsible for loading built-in modules
and plugins, as well as acting as the "bridge" between protected data in bootscript
context and modules.

For the bridge protocol, check Beacon (shinobu/beacon)
